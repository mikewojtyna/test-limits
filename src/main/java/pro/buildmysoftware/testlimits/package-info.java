/**
 *
 */
/**
 * A set of code samples showing limits of unit testing. There are two
 * sub-packages: "good" and "bad". Both packages contain the same samples.
 * However, "good" package contains samples designed properly, while "bad"
 * samples are badly designed (i.e. hard to test) or have bugs.
 *
 * @author goobar
 *
 */
package pro.buildmysoftware.testlimits;